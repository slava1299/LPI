<Window 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="LPI.SN_InputWindow"
        Title="Ввод серийных номеров" WindowState="Maximized" Width="953">


    <Grid Margin="10">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Width="240">
            <!-- Комбо-бокс для выбора порта -->
            <Label Content="COM-порт:" Height="27" />
            <ComboBox Name="comboPorts" Width="95" Margin="0,0,5,0" Height="27"/>
            <Button Content="Соединить" Click="ConnectToSelectedPort" Height="27"/>

            <!-- Кнопка соединения -->
        </StackPanel>
        <Label Name="labelRecordNumber" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Top" HorizontalContentAlignment="Center"/>
        <DataGrid Name="dataGrid" Margin="10,50,10,40" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" FontSize="14" Grid.ColumnSpan="2" MouseDoubleClick="dataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Серийный номер" Binding="{Binding Snum}" MinWidth="80" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Код SL" Binding="{Binding ItemCode}" MinWidth="80" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Название" Binding="{Binding nazvanie}" MinWidth="80" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Комментарий" Binding="{Binding kommentarii}" MinWidth="80" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Подразделение" Binding="{Binding Podrazdelenie}" MinWidth="80" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Тип испытаний" Binding="{Binding Tip_isp}" MinWidth="80" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Основание" Binding="{Binding Osnovanie}" MinWidth="80" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Результат" Binding="{Binding res}" MinWidth="80" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Статус" Binding="{Binding stat}" MinWidth="80" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="Удалить" Width="Auto">
                    <DataGridTemplateColumn.CellStyle>
                        <Style TargetType="{x:Type DataGridCell}">
                            <Setter Property="Cursor" Value="Hand"/>
                            <!-- Устанавливаем форму курсора -->
                        </Style>
                    </DataGridTemplateColumn.CellStyle>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="pictures/deleteSN.png" Stretch="Uniform" MaxHeight="64" MaxWidth="64"
                                   MouseLeftButtonDown="OnDeleteIconClicked"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <TextBox x:Name="txtBarcode" HorizontalAlignment="Left" Margin="126,1,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="168" Height="27" FontSize="18"/>
        <Label Content="Ввод СН:" HorizontalAlignment="Left" Margin="60,-1,0,0" VerticalAlignment="Top" FontSize="14"/>
        <Button HorizontalAlignment="Left" Margin="10,-1,0,0" VerticalAlignment="Top" Height="44" Width="50" Background="White" BorderBrush="White" Click="Button_Click">
            <Image Source="/pictures/back_ward.png" Stretch="Uniform" />
        </Button>



    </Grid>
</Window>
